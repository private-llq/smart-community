<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsy.community.mapper.CommonConstMapper">

    <select id="listByType" resultType="java.lang.Long">
        SELECT id FROM `t_common_const` where type_id =#{i}
    </select>

</mapper>
