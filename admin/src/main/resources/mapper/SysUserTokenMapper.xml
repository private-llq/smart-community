<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jsy.community.mapper.SysUserTokenMapper">
	<select id="queryByToken" resultType="com.jsy.community.entity.sys.SysUserTokenEntity">
		select *
		from t_sys_user_token
		where token = #{value}
	</select>
</mapper>